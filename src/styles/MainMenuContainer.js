import styled from "styled-components";

export const MainMenuContainer = styled.nav`
    ul{
        list-style:none;
        padding:0;
    }

    li{
        display: inline-block;
        padding: 0 5px;
    }
`